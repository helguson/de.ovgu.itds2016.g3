#pragma once


class ThreeDTree {
public:
	// std::vector<Point3D> query(const ? range) const;
	// std::vector<Point3D> query(const Point3d point, const double distance) const;
	// Point3D estimateNearNeighborOf(const Point3d point) const;

private:
	// std::shared_ptr<Node> _root;
};